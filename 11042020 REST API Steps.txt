Step by step REST API Implementation


1. Create test table
	CREATE TABLE `test` (
		`id` INT(11) NOT NULL AUTO_INCREMENT,
		`name` VARCHAR(200) NOT NULL COLLATE 'utf8mb4',
		`age` INT(11) NOT NULL,
		`email` VARCHAR(50) NOT NULL COLLATE 'utf8mb4',
		PRIMARY KEY (`id`) USING BTREE
	)
	COLLATE='utf8mb4'
	ENGINE=InnoDB
	;
	
2.	Create POJO in domain
	- Alt + shift + s for Getter/Setters and toString
	
3.	Create repository
	- Ctrl + shift + 0 will rearrange the import statements
	- Try to use List<T>

4.	Create Service
	- Copy paste same statements within the repository
	
5.	Create impl
	- path : /service/impl/
	- implement all the methods of service interface here
	- simply each method will return repository.findByXYZ();
	
6.  Create Controller and required end-points
